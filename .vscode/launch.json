{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Current ts file",
      "type": "node",
      "request": "launch",
      "program": "${workspaceRoot}/node_modules/.bin/tsx",
      "args": [
        "${relativeFile}"
      ],
      "cwd": "${workspaceRoot}"
    }
  ]
}